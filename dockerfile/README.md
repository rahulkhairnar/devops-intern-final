# Hello World Docker

## Description
This project contains a simple Python script that prints "Hello, World!" and is containerized using Docker.

## Files
- `hello.py`: A simple Python script that prints a greeting.
- `Dockerfile`: The Docker configuration used to build the container.

## Instructions to Build and Run

1. **Build the Docker Image**
   ```bash
   docker build -t hello-world .

