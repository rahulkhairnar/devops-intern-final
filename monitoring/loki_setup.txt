Loki setup notes
Author: Rahul
Date: $(date -u +"%Y-%m-%d")
Summary:
- Run Loki via Docker
- Config file: loki-config.yaml
- View logs via: curl http://localhost:3100/loki/api/v1/query?query={job="hello"}
Run instructions:
1) docker run -d --name loki -p 3100:3100 -v $(pwd)/loki-config.yaml:/etc/loki/local-config.yaml grafana/loki:2.9.0 -config.file=/etc/loki/local-config.yaml
